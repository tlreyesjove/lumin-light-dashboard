# Python
.venv/
__pycache__/
*.pyc

# Google service account credentials — never commit these
credentials/*.json
.env

# Generated data files (regenerate with scripts/generate_data.py — Google Sheets is the source of truth)
data/*.csv

# Design reference screenshots (e.g. from Pipedrive, NetSuite) used to guide
# chart design — personal reference material, not project deliverables, and
# some are screenshots of real third-party products.
data/ERP Inspiration/

# Excel lock files (created while any .xlsx in this repo is open)
~$*.xlsx

# OS junk
.DS_Store

# Old internal-facing README draft — working notes between Tatiana and
# Claude Code, not meant for external readers. Never publish this.
INTERNAL_OLD_README.md

# Personal working notes, not meant for the public repo — kept locally only.
Learning Doc.md
INTERNAL_Lumin Light Project Spec.md
